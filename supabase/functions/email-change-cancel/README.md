# email-change-cancel

Cancels a pending email-change state by forcing the current email as confirmed and clearing `user_metadata.familiada_email_change_pending`.

Env required: `SUPABASE_SERVICE_ROLE_KEY`.
